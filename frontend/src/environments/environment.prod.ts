// Used when you build for production (ng build / ionic build)
export const environment = {
  production: true,

  firebase: {
    apiKey: 'YOUR_FIREBASE_API_KEY',        // // Put the production key (or same)
    projectId: 'YOUR_FIREBASE_PROJECT_ID',
  },

  googleMapsApiKey: 'YOUR_GOOGLE_MAPS_API_KEY',
};
